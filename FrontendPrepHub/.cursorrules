# FrontendPrepHub 项目 Cursor 规则

## 项目概述

这是一个前端面试备战平台（FrontendPrepHub），采用前后端分离架构：
- **后端**：Express.js + MongoDB + TypeScript
- **前端**：React + TypeScript + Vite + Tailwind CSS

## 项目结构

```
FrontendPrepHub/
├── backend/                 # 后端代码
│   └── src/
│       ├── config/          # 配置文件（数据库、环境变量）
│       ├── controllers/     # 控制器（业务逻辑）
│       ├── middleware/      # 中间件（认证、错误处理、验证）
│       ├── models/          # Mongoose 数据模型
│       ├── routes/          # API 路由定义
│       ├── scripts/         # 脚本（数据库种子等）
│       └── index.ts         # 应用入口
├── frontend/                # 前端代码
│   └── src/
│       ├── layouts/         # 布局组件
│       ├── pages/           # 页面组件
│       ├── router/          # 路由配置
│       ├── services/        # API 服务
│       ├── store/           # 状态管理 (Zustand)
│       ├── styles/          # 全局样式
│       ├── types/           # TypeScript 类型定义
│       ├── App.tsx          # 根组件
│       └── main.tsx         # 入口文件
└── docs/                    # 文档
```

## 技术栈规范

### 后端技术栈
- **运行时**：Node.js 18+
- **框架**：Express.js
- **数据库**：MongoDB + Mongoose
- **语言**：TypeScript（严格模式）
- **认证**：JWT (jsonwebtoken)
- **密码加密**：bcryptjs
- **验证**：express-validator
- **安全**：helmet, cors

### 前端技术栈
- **框架**：React 18+
- **构建工具**：Vite
- **语言**：TypeScript
- **样式**：Tailwind CSS
- **状态管理**：Zustand
- **路由**：React Router v6
- **HTTP 客户端**：Axios
- **代码编辑器**：Monaco Editor
- **Markdown 渲染**：react-markdown + react-syntax-highlighter

## 编码规范

### 通用规范
1. 所有代码必须使用 TypeScript，启用严格模式
2. 所有代码必须添加详细的中文注释
3. 遵循 ESLint 和 Prettier 配置
4. 使用 async/await 处理异步操作，避免回调地狱

### 命名规范
- **文件名**：
  - 组件：PascalCase（如 `UserProfile.tsx`）
  - 工具/服务：camelCase（如 `api.ts`）
  - 类型定义：camelCase 或 PascalCase
- **变量/函数**：camelCase（如 `getUserById`）
- **常量**：UPPER_SNAKE_CASE（如 `MAX_RETRY_COUNT`）
- **接口/类型**：PascalCase，接口以 `I` 开头（如 `IUser`）
- **枚举**：PascalCase（如 `UserRole`）

### 后端规范
1. 控制器方法使用 `asyncHandler` 包装，统一错误处理
2. 使用 `AppError` 类抛出业务错误
3. API 响应格式统一：`{ success: boolean, message?: string, data?: T }`
4. 路由验证使用 express-validator 中间件
5. 敏感操作需要认证中间件（`authenticate`）
6. 会员内容需要权限中间件（`requireMember`）

### 前端规范
1. 组件使用函数式组件 + Hooks
2. 状态管理优先使用 Zustand
3. API 调用统一通过 `services/api.ts`
4. 样式使用 Tailwind CSS 工具类
5. 路由守卫使用 `ProtectedRoute` 组件
6. 表单验证在组件内处理

## 注释规范

所有代码必须添加中文注释，格式如下：

### 文件头注释
```typescript
/**
 * 文件功能说明
 * 主要用途描述
 */
```

### 函数/方法注释
```typescript
/**
 * 函数功能说明
 * @param paramName - 参数说明
 * @returns 返回值说明
 */
```

### 代码块注释
```typescript
// ==================== 模块分隔 ====================
// -------------------- 子模块分隔 --------------------
```

## API 设计规范

### RESTful 路由
- GET `/api/resource` - 获取列表
- GET `/api/resource/:id` - 获取详情
- POST `/api/resource` - 创建
- PUT `/api/resource/:id` - 更新
- DELETE `/api/resource/:id` - 删除

### 认证路由
- POST `/api/auth/register` - 注册
- POST `/api/auth/login` - 登录
- GET `/api/auth/me` - 获取当前用户
- PUT `/api/auth/profile` - 更新资料
- PUT `/api/auth/password` - 修改密码

### 响应格式
```typescript
// 成功响应
{
  success: true,
  data: { ... },
  message?: "操作成功"
}

// 错误响应
{
  success: false,
  message: "错误信息",
  code?: "ERROR_CODE",
  errors?: [{ field: "email", message: "邮箱格式错误" }]
}

// 分页响应
{
  success: true,
  data: {
    items: [...],
    pagination: {
      page: 1,
      limit: 10,
      total: 100,
      totalPages: 10
    }
  }
}
```

## 数据模型

### 用户角色
- `user` - 普通用户
- `member` - 会员用户（可访问付费内容）
- `admin` - 管理员

### 内容权限
- 基础内容：所有用户可访问
- 高级内容（`isFree: false` 或 `level: advanced`）：仅会员可访问

## UI/UX 规范

### 颜色系统
- **主色 (primary)**：蓝色系 (#3b82f6)
- **强调色 (accent)**：紫色系 (#a855f7)
- **成功 (success)**：绿色 (#22c55e)
- **警告 (warning)**：橙色 (#f59e0b)
- **错误 (danger)**：红色 (#ef4444)
- **深色背景 (dark)**：#020617 - #f8fafc

### 组件类名
- 按钮：`btn`, `btn-primary`, `btn-secondary`, `btn-ghost`, `btn-danger`
- 输入框：`input`, `input-error`
- 卡片：`card`, `card-hover`
- 徽章：`badge`, `badge-primary`, `badge-success`, `badge-warning`, `badge-danger`

### 响应式断点
- `sm`: 640px
- `md`: 768px
- `lg`: 1024px
- `xl`: 1280px

## 安全规范

1. 密码使用 bcrypt 加密，成本因子 ≥ 10
2. JWT 过期时间配置在环境变量中
3. 敏感信息不记录到日志
4. API 请求使用 CORS 白名单
5. 使用 Helmet 设置安全响应头
6. 输入验证使用 express-validator
7. MongoDB 查询防注入

## Git 提交规范

### 远程仓库配置

**重要**：本项目是 [AIProjects](https://github.com/ZxdNoob/AIProjects) 仓库的子项目。

| 配置项 | 值 |
|-------|-----|
| **父仓库地址** | https://github.com/ZxdNoob/AIProjects |
| **子项目路径** | `FrontendPrepHub/` |
| **子项目地址** | https://github.com/ZxdNoob/AIProjects/tree/master/FrontendPrepHub |
| **远程名称** | `upstream` |

### 代码推送规范

**重要**：本项目使用 `git subtree` 方式管理，推送代码时必须遵循以下规则：

1. **推送到子目录**：推送时必须使用 `git subtree push` 命令，确保只更新远程仓库中的 `FrontendPrepHub` 目录
2. **禁止直接推送**：禁止使用 `git push upstream master` 直接推送，这会覆盖父仓库中的其他子项目
3. **推送命令格式**：
   ```bash
   # 正确的推送方式（从项目根目录执行）
   git subtree push --prefix=. upstream master
   
   # 或者先在本地提交，然后推送到父仓库的 FrontendPrepHub 目录
   git push upstream `git subtree split --prefix=.`:refs/heads/master
   ```

4. **拉取更新**：
   ```bash
   # 从父仓库拉取 FrontendPrepHub 目录的更新
   git subtree pull --prefix=. upstream master --squash
   ```

### 推送注意事项

1. **确认远程仓库状态**：推送前先检查远程仓库 https://github.com/ZxdNoob/AIProjects 的当前状态
2. **避免覆盖其他项目**：父仓库包含多个子项目（如 `zhishuaizi` 等），推送时务必只影响 `FrontendPrepHub` 目录
3. **合并冲突处理**：如遇到冲突，应先拉取远程更新并在本地解决冲突后再推送

### 提交权限规则
**重要**：AI 助手在没有收到用户明确的代码提交指令时，**禁止自动提交代码**。只有在用户明确要求提交（如"提交代码"、"commit"、"git commit"等）时才能执行提交操作。

### 提交日志格式
所有提交信息必须遵循以下格式：

```
<emoji> <type>(<scope>): <description>: <details>
```

**格式说明**：
- **emoji**：提交类型对应的 emoji 图标（必需）
- **type**：提交类型（必需）
- **scope**：影响范围（可选，用括号包裹）
- **description**：简短说明（必需，中文）
- **:**：分隔符（必需）
- **details**：详细更新内容（可选，中文）

### 提交类型和 Emoji

| 类型 | Emoji | 说明 | 示例 |
|------|-------|------|------|
| feat | ✨ | 新功能 | `✨ feat(auth): 添加用户登录功能: 实现邮箱密码登录和 JWT 认证` |
| fix | 🐛 | 修复 Bug | `🐛 fix(api): 修复分页查询错误: 修正 limit 参数未生效的问题` |
| docs | 📝 | 文档更新 | `📝 docs(readme): 更新项目说明: 添加部署步骤和配置说明` |
| style | 💄 | 代码格式 | `💄 style(components): 统一代码格式: 使用 Prettier 格式化所有组件` |
| refactor | ♻️ | 代码重构 | `♻️ refactor(store): 重构状态管理: 将 authStore 拆分为多个模块` |
| perf | ⚡ | 性能优化 | `⚡ perf(api): 优化查询性能: 添加数据库索引减少查询时间` |
| test | ✅ | 测试相关 | `✅ test(utils): 添加工具函数测试: 覆盖所有边界情况` |
| chore | 🔧 | 构建/工具 | `🔧 chore(deps): 更新依赖版本: 升级 React 到 18.2.0` |
| build | 📦 | 构建系统 | `📦 build(vite): 优化构建配置: 启用代码分割和压缩` |
| ci | 👷 | CI/CD | `👷 ci(github): 添加自动化测试: 配置 GitHub Actions` |
| revert | ⏪ | 回滚提交 | `⏪ revert: 回滚上次提交: 修复引入的严重 Bug` |

### 提交示例

```bash
# 新功能
✨ feat(backend): 添加知识点收藏功能: 用户可以将知识点添加到收藏列表

# 修复 Bug
🐛 fix(frontend): 修复登录页面验证错误: 修正邮箱格式验证正则表达式

# 文档更新
📝 docs: 更新 API 文档: 添加用户管理接口说明

# 代码格式
💄 style: 统一代码格式: 使用 ESLint 和 Prettier 格式化所有文件

# 重构
♻️ refactor(controllers): 重构认证控制器: 提取公共逻辑到工具函数

# 性能优化
⚡ perf(database): 优化查询性能: 为常用查询字段添加索引

# 测试
✅ test(middleware): 添加认证中间件测试: 覆盖所有认证场景

# 构建工具
🔧 chore: 更新依赖版本: 升级 Express 到 4.18.2

# 多行提交信息（详细说明）
✨ feat(admin): 添加用户管理功能: 
- 实现用户列表查询和筛选
- 添加用户角色修改功能
- 支持批量操作用户状态
```

### 提交规范要求
1. 提交信息必须使用中文
2. 第一行不超过 50 个字符（emoji + type + scope + description）
3. 详细说明可以换行，每行不超过 72 个字符
4. 使用现在时态（"添加"而不是"添加了"）
5. 首字母小写（除非是专有名词）
6. 结尾不加句号

## 版本号管理规范

### 版本号规则
**重要**：AI 助手**禁止自动更新版本号**。版本号必须严格遵循语义化版本（Semantic Versioning）规范：`x.y.z`

**版本号格式**：`主版本号.次版本号.修订号`（如：`1.2.3`）

### 版本号变更规则

| 版本号部分 | 变更条件 | 示例 | 说明 |
|-----------|---------|------|------|
| **主版本号 (x)** | 不兼容的 API 修改 | `1.0.0` → `2.0.0` | 重大变更、破坏性更新 |
| **次版本号 (y)** | 向后兼容的功能新增 | `1.0.0` → `1.1.0` | 新功能、新特性 |
| **修订号 (z)** | 向后兼容的问题修复 | `1.0.0` → `1.0.1` | Bug 修复、补丁 |

### 版本号更新流程

1. **禁止自动更新**：AI 助手不得自动修改 `package.json` 中的版本号
2. **独立判断**：根据变更范围判断需要更新哪个版本号（前端或后端），两者完全独立
3. **询问用户**：当功能发生明确变更时（如新增功能、修复 Bug、重大重构），可以询问用户是否需要更新对应的版本号
4. **用户决定**：版本号更新必须由用户明确决定和确认
5. **遵循规范**：如果用户同意更新，必须严格按照语义化版本规范递增
6. **同步文档**：更新版本号时，必须同步更新相关文档（如 README.md、CHANGELOG.md 等）中的版本号信息

### 版本号更新场景

#### 需要询问用户是否更新版本号的场景：
- ✨ **新增功能**（feat）：建议更新次版本号（y）
- 🐛 **修复 Bug**（fix）：建议更新修订号（z）
- ♻️ **重大重构**（refactor）：根据影响范围决定主版本号（x）或次版本号（y）
- ⚡ **性能优化**（perf）：通常更新修订号（z），重大优化可更新次版本号（y）
- 🔧 **依赖更新**（chore）：根据依赖变更影响决定是否更新版本号

#### 通常不需要更新版本号的场景：
- 📝 **文档更新**（docs）
- 💄 **代码格式**（style）
- ✅ **测试相关**（test）
- 🔧 **构建配置**（build/chore，不影响功能）

### 版本号更新示例

```bash
# 场景 1：前端新增用户收藏功能（仅前端变更）
# AI 应询问："前端新增了知识点收藏功能，是否需要将前端版本号从 1.0.0 更新到 1.1.0？"
# 用户同意后：更新 frontend/package.json 中的 "version": "1.1.0"
# 注意：后端版本号不受影响

# 场景 2：后端修复登录验证 Bug（仅后端变更）
# AI 应询问："后端修复了登录验证错误，是否需要将后端版本号从 1.0.0 更新到 1.0.1？"
# 用户同意后：更新 backend/package.json 中的 "version": "1.0.1"
# 注意：前端版本号不受影响

# 场景 3：后端重大 API 变更（仅后端变更）
# AI 应询问："后端 API 路由结构发生重大变更，不向后兼容，是否需要将后端版本号从 1.1.1 更新到 2.0.0？"
# 用户同意后：更新 backend/package.json 中的 "version": "2.0.0"
# 注意：前端版本号不受影响

# 场景 4：同时修改前后端（前后端独立判断）
# 前端：新增了用户收藏 UI 组件
# 后端：新增了收藏 API 接口
# AI 应分别询问：
#   - "前端新增了用户收藏 UI 组件，是否需要将前端版本号从 1.0.0 更新到 1.1.0？"
#   - "后端新增了收藏 API 接口，是否需要将后端版本号从 1.0.0 更新到 1.1.0？"
# 用户分别决定后，独立更新各自的版本号
```

### 版本号独立管理

**重要**：前端版本号和后端版本号**完全独立**，互不影响。

1. **前端版本号**：
   - 只在前端代码发生值得更新版本号的变更时更新
   - 前端功能新增、Bug 修复、UI 优化等前端相关变更才考虑更新前端版本号
   - 后端变更不影响前端版本号

2. **后端版本号**：
   - 只在后端代码发生值得更新版本号的变更时更新
   - 后端 API 变更、业务逻辑调整、数据库模型变更等后端相关变更才考虑更新后端版本号
   - 前端变更不影响后端版本号

3. **独立判断**：
   - 当只修改前端代码时，只询问是否需要更新前端版本号
   - 当只修改后端代码时，只询问是否需要更新后端版本号
   - 当同时修改前后端代码时，分别判断是否需要更新各自的版本号

### 版本号文件位置

- **后端**：`backend/package.json` 中的 `"version"` 字段
- **前端**：`frontend/package.json` 中的 `"version"` 字段

### 注意事项

1. 版本号必须严格遵循 `x.y.z` 格式，不得使用其他格式（如 `1.0`、`v1.0.0` 等）
2. 版本号递增必须遵循语义化版本规范，不得随意跳跃
3. 主版本号和次版本号更新时，修订号应重置为 0（如：`1.2.3` → `2.0.0`）
4. 修订号更新时，其他部分保持不变（如：`1.2.3` → `1.2.4`）

### Git Tag 规范

**重要**：当版本号更新时，必须同时创建对应的 Git Tag。

#### Tag 命名规则

由于本项目是父仓库 `AIProjects` 的子项目，Tag 需要包含子项目名称，并区分前后端：

| 端 | Tag 格式 | 示例 |
|-----|---------|------|
| **前端** | `FrontendPrepHub-frontend-v{version}` | `FrontendPrepHub-frontend-v1.0.1` |
| **后端** | `FrontendPrepHub-backend-v{version}` | `FrontendPrepHub-backend-v1.0.1` |

#### Tag 创建流程

1. **提交代码后创建 Tag**：在代码提交完成后，为更新的版本创建 Tag
2. **推送 Tag 到远程**：创建 Tag 后需要推送到远程仓库
3. **Tag 消息**：Tag 消息应包含版本更新的简要说明

#### Tag 命令示例

```bash
# 创建前端版本 Tag
git tag -a FrontendPrepHub-frontend-v1.0.1 -m "🏷️ FrontendPrepHub 前端 v1.0.1: 修复开发环境问题"

# 创建后端版本 Tag
git tag -a FrontendPrepHub-backend-v1.0.1 -m "🏷️ FrontendPrepHub 后端 v1.0.1: 修复 MongoDB 连接和 TypeScript 编译问题"

# 推送单个 Tag 到远程（在父仓库 AIProjects 中执行）
git push origin FrontendPrepHub-frontend-v1.0.1
git push origin FrontendPrepHub-backend-v1.0.1

# 或推送所有 Tag 到远程
git push origin --tags
```

#### Tag 管理命令

```bash
# 查看所有 Tag
git tag -l

# 查看 FrontendPrepHub 项目的 Tag
git tag -l "FrontendPrepHub-*"

# 查看特定端的 Tag
git tag -l "FrontendPrepHub-frontend-*"
git tag -l "FrontendPrepHub-backend-*"

# 查看 Tag 详细信息
git show FrontendPrepHub-frontend-v1.0.1

# 删除本地 Tag（如有错误）
git tag -d FrontendPrepHub-frontend-v1.0.1

# 删除远程 Tag（如有错误）
git push origin --delete FrontendPrepHub-frontend-v1.0.1
```

#### 注意事项

1. **版本号更新必须创建 Tag**：每次更新版本号后，必须创建对应的 Git Tag
2. **前后端分别创建**：如果同时更新前后端版本号，需要分别创建两个 Tag
3. **Tag 消息规范**：Tag 消息使用 `🏷️ FrontendPrepHub {端} v{版本号}: {简要说明}` 格式
4. **在父仓库创建 Tag**：由于本项目是 AIProjects 的子项目，Tag 需要在父仓库中创建和推送
5. **推送 Tag**：创建 Tag 后必须推送到远程仓库，确保版本追溯

## 文档管理规范

### 文档更新时间规范

**重要**：当文档需要记录更新时间时，必须遵循以下规范：

1. **独立执行 date 命令**：**每个文档必须独立执行 `date` 命令获取当前时间**，不得使用之前获取的统一时间。每次更新文档时，都要重新执行 `date` 命令获取最新的时间。
2. **时间格式**：更新时间必须精确到秒，格式为 `YYYY-MM-DD hh:mm:ss`（如：`2024-01-15 14:30:25`）
3. **时间获取**：使用系统命令 `date "+%Y-%m-%d %H:%M:%S"` 获取格式化的时间字符串
4. **执行时机**：在更新每个文档的更新时间字段前，必须立即执行 `date` 命令，确保时间准确反映文档的实际更新时间
5. **应用场景**：
   - 更新 CHANGELOG.md 时记录版本发布时间
   - 更新 API 文档时记录文档最后更新时间
   - 更新 README.md 时记录项目最后更新时间
   - 更新源码解读文档时记录文档更新时间
   - 更新学习笔记时记录文档更新时间
   - 更新学习心得时记录文档更新时间
   - 其他需要记录时间的文档

### 文档更新时间示例

```bash
# 错误示例：统一获取时间后用于多个文档（禁止）
date "+%Y-%m-%d %H:%M:%S"  # 获取一次时间
# 然后用于多个文档... ❌ 错误做法

# 正确示例：每个文档独立执行 date 命令
# 更新文档 A 时
date "+%Y-%m-%d %H:%M:%S"  # 输出：2024-01-15 14:30:25
# 在文档 A 中使用：更新时间：2024-01-15 14:30:25

# 更新文档 B 时（即使在同一批次更新）
date "+%Y-%m-%d %H:%M:%S"  # 输出：2024-01-15 14:30:28（时间可能不同）
# 在文档 B 中使用：更新时间：2024-01-15 14:30:28

# 在文档中使用
最后更新时间：2024-01-15 14:30:25
```

## 开发流程

1. **开发前**：阅读相关代码，理解现有架构
2. **开发中**：遵循上述规范，添加完整注释
3. **开发后**：运行 lint 检查，确保无错误
4. **提交前**：确认代码符合规范，编写规范的提交信息

## 常用命令

### 后端
```bash
cd backend
npm run dev      # 开发模式
npm run build    # 构建
npm run seed     # 初始化数据
```

### 前端
```bash
cd frontend
npm run dev      # 开发模式
npm run build    # 构建
npm run preview  # 预览构建结果
```

## 注意事项

1. 修改数据模型后需要同步更新前端类型定义
2. 新增 API 需要同时更新前端 services/api.ts
3. 路由变更需要同步更新前端 router/index.tsx
4. 环境变量变更需要更新 env.example.txt
5. 所有用户可见的文本使用中文

